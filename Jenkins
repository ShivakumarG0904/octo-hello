pipeline {
    agent any

    stages {
        stage('building system details') {
            steps {
                echo "systemdeatis first stage"
                uname -a
            }
        }
    }
}
